% xCompareMaxima.m

% For comparing grid-search maxima data for different Tau configurations
% 1 August 2018

clear;
close all;

% true values
t_DBV = 0.0400;
t_R2p = 5.6799;

% array of true values
trues = repmat([t_R2p;t_DBV],1,5);

% load a dataset
load('GridMaxima_14t_0to65in5.mat');

% calculate differences
diffs = abs(maxes-trues);
mdiff = mean(diffs,2);

% display results
